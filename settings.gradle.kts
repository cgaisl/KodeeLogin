pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

rootProject.name = "KodeeLogin"

include(":common")
include(":desktop")
include(":android")
include(":web")
